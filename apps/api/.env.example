# DATABASE_URL is not read by anything yet — Prisma arrives in stage 3.
# It is defined now so the connection string stays in sync with docker-compose.yml.
DATABASE_URL=postgresql://pnewmo:pnewmo_local_dev@localhost:5433/pnewmo?schema=public
PORT=4000
WEB_ORIGIN=http://localhost:3000
