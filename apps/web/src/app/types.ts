// В эталоне сами page.tsx/error.tsx оставляют Props inline (готового
// прецедента с сиблинг-файлом типов рядом с ними не нашлось) — но заказчик
// прямо включил app/ в объём переноса, поэтому решение здесь моё: сиблинг
// `types.ts` в каждом сегменте роута, по фиксированному имени файла типов
// из конвенции эталона.

export interface ErrorPageProps {
  error: Error & { digest?: string };
  reset: () => void;
}
